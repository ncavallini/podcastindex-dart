/// Utility class that contains constants used throughout the library.
class Constants {
  /// The default user agent to use for HTTP requests.
  static const String defaultUserAgent = 'SomethingAwesome/1.0.1';

  /// Prevents creation of [Constants] instances.
  Constants._();
}
